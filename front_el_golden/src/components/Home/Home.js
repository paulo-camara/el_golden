import React from 'react';
import { Menu } from '../Menu/Menu';

export const Home = () => {
    return (
        <div className="color-background-page home-page">
            <Menu />
        </div>
    );
};
